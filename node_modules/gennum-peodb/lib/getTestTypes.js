//============================================================================//

var print = console.log;

//============================================================================//

module.exports = function(PEODB) {

  var qStr =
    'SELECT'
  + '    tp_type_id'
  + '    , abbr'
  + '    , description'
  + '  FROM'
  + '    tp_types'
  ;

  PEODB.prototype.getTestTypes = function(args, cb) {
    var peodb = this;

    if (typeof(args) === 'function') {
      cb = args;
    }

    peodb.sharedDB.queryData(qStr, cb);
  }
}

//============================================================================//
