################################################################################

CREATE TABLE `daemon_mutexes` (
  `daemon_name`         VARCHAR(64)  NOT NULL PRIMARY KEY
, `start_time`          DOUBLE       NOT NULL
, `hostname`            VARCHAR(32)  NOT NULL
, `pid`                 INT UNSIGNED NOT NULL
, UNIQUE KEY (`daemon_name`)
);

################################################################################
