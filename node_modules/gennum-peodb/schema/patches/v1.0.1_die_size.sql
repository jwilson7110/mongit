################################################################################
#
#  Remove the `part_dies.die_size` field, because it's redundant
#  (that table now has the `die_size_x` and `die_size_y` fields.
#
#  Change the `config` table's 'version' value to have a 'v' prefix.
#
################################################################################

USE peo;

START TRANSACTION;

UPDATE config SET value = 'v1.0.1' WHERE `key` = 'version';

################################################################################


ALTER TABLE part_dies DROP COLUMN die_size;


################################################################################

COMMIT;

################################################################################
