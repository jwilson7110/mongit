################################################################################

CREATE TABLE `prober_types` ( -- LIST-TABLE
  `prober_type_id`      INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY
, `name`                VARCHAR(255) NOT NULL
, UNIQUE KEY (`name`)
);

################################################################################
